- XML hat noch dependency auf sourceamazing-schema (wegen logger/fileAccess)
 -> ev. logger/fileAccess als ServiceProvider anbieten (wie geht das in Tests?)
- Type-Package aufräumen
- Proxy entweder function oder properties based? (zwei Implementierungen)?

- Code verschönern
  vorher if(!isInterface(class)) throw Exception)
  nachher: checkisNotInterface(class) -> Hauptlogik (z.B. Validierung) lesen können wie ein Buch!

- Tests für neue ExpectedRootAlias-Annotatation
- Tests in schema für public APIs? Was ist schon da?
- Think about: ExpectedRootAlias-Annotation ersetzen durch ExpectedAliasFromSuperiorBuilder
 -> Builder ev. direkt mit Root Konzept initialisieren (auf TopLevel) bzw. Optionaler Root mitgegeben

- TODOs mit neuer DataCollector API

- Redundante Interfaces in Tests aufräumen? (Schema -> Concept)

- Naming: Concept/Facet ersetzen mit Instance/Property ?
- Naming: Schema ersetzen bzw. aufräumen


- XML Annotationen
- Klassen mit Konstruktor unterstützen (data class)
